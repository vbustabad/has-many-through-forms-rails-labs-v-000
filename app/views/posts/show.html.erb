<h1><%= @post.title %></h1>

<p><%= @post.content %></p>

Comments:

<div>
  <%= @post.comments.each do |comment| %>
    <%= comment.content %>
  <% end %>
</div>

Users:

<ul>
  <%= @post.users.each do |user| %>
    <li><%= link_to user.username, user_path(user) %></li>
  <% end %>
</ul>
